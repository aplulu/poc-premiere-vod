{
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format-check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "dependencies": {}
}
